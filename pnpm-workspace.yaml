allowBuilds:
  # sentry-cli binary is only needed for source map upload; we don't upload.
  '@sentry/cli': false
  esbuild: false
  msw: false
  sharp: false
  unrs-resolver: false
minimumReleaseAge: 0
overrides:
  esbuild: ^0.25.12
  postcss: ^8.5.14
